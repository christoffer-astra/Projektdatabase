@model IEnumerable<UddOmrModel>
@{
    ViewData["Title"] = "Projektdatabasen";
}

<h2>Projektdatabasen</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UddOmrName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UddOmrId)
        </th>
        <th></th>
    </tr>


    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UddOmrName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UddOmrId)
            </td>
            @*<td>
                @Html.ActionLink("Edit", "Edit", new { id = item.UddOmrId }) |
                @Html.ActionLink("Details", "Details", new { id = item.UddOmrId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.UddOmrId })

            </td>*@
        </tr>
    }
</table>